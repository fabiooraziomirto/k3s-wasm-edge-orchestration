apiVersion: wasmbed.github.io/v0
kind: Device
metadata:
  name: riscv-board-3
  namespace: wasmbed
spec:
  # PLACEHOLDER, not a usable identity. The real value is the device's
  # SubjectPublicKeyInfo in url-safe base64, and the device must hold the
  # matching P-256 private key to answer the gateway's enrollment challenge.
  # Generate both with:  ./scripts/provision-device-identity.sh <device-name>
  publicKey: "riscv-board-3-public-key-12345"
status:
  phase: "Enrolled"
  gateway: "gateway-3"
  connectedSince: "2025-09-27T17:00:00Z"
  lastHeartbeat: "2025-09-27T17:00:00Z"
  pairingMode: false
